import { useParams } from "react-router-dom";

export default async function Pasirinkti(){
  // // const { miestas } = useParams()
  //   const data = await fetch(`https://cors-anywhere.herokuapp.com/https://api.meteo.lt/v1/places/${miestas}/forecasts/long-term`);
  //   const json = json.json();
  //   const items = [];
  //   const keys = ["airTemperature","feelsLikeTemperature","windSpeed","windDirection","cloudCover","conditionCode"];

  // // data.forEach(forecast => {
  // //   const item = {};
  // //   keys.forEach(key => {
  // //       // Check if the key exists in the forecast data
  // //       if (forecast.hasOwnProperty(key)) {
  // //           // Add the value to the items array
  // //           items.push(forecast[key]);
  // //       }
  // //   });
  // // });
  return(
    <>
      hello
    </>
  )
}